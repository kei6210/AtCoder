//snippet
#include <bits/stdc++.h>
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <math.h>
#include <iomanip>
#define rep(i,a,b) for(int i=a;i<b;i++)
#define rrep(i,a,b) for(int i=a;i>=b;i--)
#define fore(i,a) for(auto &i:a)
#define INF32 2147483647 //2.147483647×10^{9}:32bit整数のinf
#define INF64 9223372036854775807 //9.223372036854775807×10^{18}:64bit整数のinf
#pragma GCC optimize ("-O3")
using namespace std;

typedef long long ll;

int main(){
	//cin高速化,最適化したいなら-O2オプション,最速はscanf
	//flush防止,nullptr→0でもOK
	//C,C++の入出力同期解除
	std::cin.tie(nullptr);
    ios_base::sync_with_stdio(false);

    //よくある入力
    int n,w; cin>>n>>w;
	vector<int> a(n); //動的確保
	for(int i=0;i<n;i++) cin >> a[i];

	//getlineでの入力とcinを併用するとき
	cin.clear(); cin.ignore();
	string s; getline(cin,s);

	for(ll i=n0;i*i<n;i+=2)

	str.substr(開始位置,取り出す長さ);
	str.substr(i,n-i);
	vec.push_back("XYZ");
	vec.pop_back();
	vec.push_front("ABC");
	vec.pop_front();

    //小数の桁数の出力指定
    cout<<fixed<<setprecision(16)<<INF32<<"\n"; //endlはflushするため遅い

    return 0;//なくても動作する
}